import {} from "../constants";
const initState = {};


const login  = (state = initState,action = {})=>{
    switch (action.type) {
        case "":
            
            break;
    
        default:
            return state;
    }
}

export default login;